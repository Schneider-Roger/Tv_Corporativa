const db = require("../config/db");

async function listar() {
  // Implementação básica: retorna lista vazia até DB/schema estar pronto
  // Futuramente: SELECT * FROM devices ORDER BY nome ASC
  return [];
}

module.exports = { listar };
